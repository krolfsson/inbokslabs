PORT=8080

# Optional: reject webhooks without this header value
WEBHOOK_SECRET=change-me

# SMTP (for replying with PNG). Example: Resend, Mailgun, Postmark, Google Workspace.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=apikey
SMTP_PASSWORD=secret
SMTP_FROM="Lithmuth <mockups@yourdomain.com>"
